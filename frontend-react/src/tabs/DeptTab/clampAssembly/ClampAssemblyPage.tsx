import DeptSchedulerPage from '../components/DeptSchedulerPage'
import { clampAssemblyConfig } from './config'

export default function ClampAssemblyPage() {
  return <DeptSchedulerPage config={clampAssemblyConfig} />
}
