// Re-export from shared — keep this shim so any existing imports still resolve
export {
  buildDeptRates,
  buildSteelStackRates,
  buildSteelShakeRates,
  buildClampRates,
  buildNoLoadRates,
} from '../../shared/routingRates'
