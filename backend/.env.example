PORT=3000
PGHOST=localhost
PGPORT=5432
PGDATABASE=ekarat_capacity
PGUSER=ekarat_user
PGPASSWORD=your_password

# Alternative single connection string:
# DATABASE_URL=postgres://ekarat_user:your_password@localhost:5432/ekarat_capacity

# Set to true when your cloud PostgreSQL requires SSL.
PGSSL=false
